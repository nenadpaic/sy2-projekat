{% extends '::base.html.twig' %}

{% block body -%}
    <h1>User edit</h1>

    {{ form(edit_form) }}
    <h2>Available roles to assign</h2>
    {% for role in roles %}
        {{ role.getName() }}
    {% endfor %}
    <h2>Roles</h2>
    {% for key,value in entity.getRolesName() %}
        {{ value }}
    {% endfor %}
        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_user') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
